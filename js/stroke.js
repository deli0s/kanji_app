export function mountTrace(el,kanji,onDone){const canvas=el.querySelector('canvas'),ctx=canvas.getContext('2d'),rect=()=>canvas.getBoundingClientRect();let drawing=false,points=[],last=0;function resize(){const r=rect(),d=devicePixelRatio||1;canvas.width=r.width*d;canvas.height=r.height*d;ctx.scale(d,d);ctx.lineWidth=7;ctx.lineCap='round';ctx.strokeStyle='#18212a'}resize();window.addEventListener('resize',resize,{once:true});function pos(e){const r=rect();return{x:(e.clientX-r.left),y:(e.clientY-r.top)}}canvas.addEventListener('pointerdown',e=>{drawing=true;canvas.setPointerCapture(e.pointerId);const p=pos(e);points=[p];last=Date.now();ctx.beginPath();ctx.moveTo(p.x,p.y)});canvas.addEventListener('pointermove',e=>{if(!drawing)return;const p=pos(e);points.push(p);ctx.lineTo(p.x,p.y);ctx.stroke()});canvas.addEventListener('pointerup',()=>{drawing=false;if(points.length>10){const now=Date.now();if(now-last>100){onDone?.()}}});el.querySelector('[data-clear]')?.addEventListener('click',()=>ctx.clearRect(0,0,canvas.width,canvas.height));el.querySelector('[data-stroke]')?.addEventListener('click',()=>animateStroke(canvas,kanji.strokes||[]));}
function animateStroke(canvas,strokes){const ctx=canvas.getContext('2d'),r=canvas.getBoundingClientRect();ctx.clearRect(0,0,canvas.width,canvas.height);ctx.lineWidth=7;ctx.strokeStyle='#17222a';let i=0;function one(){if(i>=strokes.length)return;const s=strokes[i++];ctx.beginPath();ctx.moveTo(s[0][0]/100*r.width,s[0][1]/100*r.height);for(const p of s.slice(1))ctx.lineTo(p[0]/100*r.width,p[1]/100*r.height);ctx.stroke();setTimeout(one,280)}one()}
